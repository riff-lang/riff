// Arrays

j = {0,1,2,3,4}     // Standard array

// Array holding anonymous functions
ops = {
    fn () = exit 0,
    fn () { r[w] = y                     ip += 3 },
    fn () { s[si++] = x                  ip += 2 },
    fn () { r[w] = s[si] delete(s[si--]) ip += 2 },
    fn () { r[w] = y == z                ip += 4 },
    fn () { r[w] = y >  z                ip += 4 }
}
